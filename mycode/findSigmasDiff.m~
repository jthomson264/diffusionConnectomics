function [ sigmas ] = findSigmasDiff( truthFile, ogFile )
% A method for finding sigmas which assumes that
% the simulated truth is correct and that all differences
% between it and the observed signal are due to rician noise.
%
% Known issue:  the aliasing seen in high b-values.

ogIm = load_nii(ogFile);
truthIm = load_nii(truthFile);




end

